Any call to get() will make a call to remote::get() and the only override for it exists in the remote. So when remote::get() is called, and the value had changed, remote will make a call to value::set() without notifying the observers
